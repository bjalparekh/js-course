class Node{
	constructor(value) {
		this.value = value;
		this.next = null;
	}
}

module.exports = {
	delete_node: function(node) {
		// write code to delete the specified node from a linked list, only the element node is given so figure out how to delete.
		// e.g
		// input = 5, 8, 3, 7, 2, 1, 9, 10, 6
		// k = 1
		// output = 5, 8, 3, 7, 2, 9, 10, 6
		throw "NotImplemented"
	},
	Node: Node,
}